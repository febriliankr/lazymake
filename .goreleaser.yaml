version: 2

builds:
  - main: .
    binary: lazymake
    ldflags:
      - -s -w -X main.version={{.Version}}
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
      - linux
      - windows
    goarch:
      - amd64
      - arm64

archives:
  - format: tar.gz
    name_template: "{{ .ProjectName }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        format: zip

brews:
  - repository:
      owner: febriliankr
      name: homebrew-tap
    homepage: https://github.com/febriliankr/lazymake
    description: Interactive TUI for Makefiles - like lazygit, but for make
    license: MIT
    install: |
      bin.install "lazymake"

checksum:
  name_template: checksums.txt

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
